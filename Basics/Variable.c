       
       /* VARIABLE = a variable is a named storage location which store some data*/

#include <stdio.h>

int main() {
    int age = 18 ;
    int price = 100 ;
    int number = 1;

    printf("age =%d\n",age);
    printf("price =%d\n",price);
    printf("number =%d\n",number);

    return 0;
}